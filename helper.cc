TF1 segC(std::vector<double> val, std::vector<double> range){
	string buf {"double"}
}

double seg(std::vector<double> val, std::vector<double (double)> range, double deft_val = 0){
	double ret = deft_val;
	for(auto lim = range.cbegin(); lim != range.cend();)
		if((lim + 1) != range.cend() && x >= *lim && x < *(++lim))
			return val[distance(range.cbegin(), lim)];
}
